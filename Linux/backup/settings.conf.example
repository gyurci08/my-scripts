# Configuration for the backup script.
# This file is sourced by backup.sh, so standard Bash syntax applies.

# An array of source directories to be backed up.
# Paths with spaces must be quoted.
SOURCE_DIRS=(
    "${HOME}/.var/app/app.zen_browser.zen/.zen/"
)

# The destination directory for the backup archives.
BACKUP_DEST="/mnt/backup"

# The filename prefix for the archive. The final name will be:
# ${FILENAME_PREFIX}_YYYYMMDD_HHMMSS.tar.${COMPRESSION_EXT}
FILENAME_PREFIX="backup"

# Compression format.
# Options: "gz" (fast, good compression), "bz2" (slower, better compression), "xz" (slowest, best compression)
COMPRESSION_FORMAT="gz"

# Retention policy: Delete backups older than this many days.
# Set to 0 to disable cleanup.
RETENTION_DAYS=30

# An array of patterns to exclude from the backup.
# Uses standard tar --exclude syntax.
EXCLUDE_PATTERNS=(
    "*.log"
    "*.tmp"
    "*.sock"
    "node_modules"
    "__pycache__"
    "vendor"
)

# Optional: Set to "true" to perform a dry run (list files to be backed up and deleted)
# without actually creating or deleting anything.
# Any other value (or if unset) will perform a real run.
DRY_RUN="false"
